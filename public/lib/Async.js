// Generated by CoffeeScript 1.3.1
(function() {

  this.Async = {
    series: function() {
      var func, funcs, idx, lastFunc, originalFunc, _i, _len;
      funcs = Array.prototype.slice.call(arguments);
      funcs.reverse();
      lastFunc;

      idx = 0;
      for (_i = 0, _len = funcs.length; _i < _len; _i++) {
        func = funcs[_i];
        if (typeof lastFunc !== "undefined" && lastFunc !== null) {
          originalFunc = func;
          (function(originalFunc, lastFunc) {
            return func = function() {
              originalFunc();
              return setTimeout(lastFunc, 1);
            };
          })(originalFunc, lastFunc);
        }
        lastFunc = func;
        idx++;
      }
      return func();
    }
  };

}).call(this);
