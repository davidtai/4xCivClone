// Generated by CoffeeScript 1.10.0
(function() {
  this.Async = {
    series: function() {
      var func, funcs, i, idx, lastFunc, len, originalFunc;
      funcs = Array.prototype.slice.call(arguments);
      funcs.reverse();
      lastFunc;
      idx = 0;
      for (i = 0, len = funcs.length; i < len; i++) {
        func = funcs[i];
        if (typeof lastFunc !== "undefined" && lastFunc !== null) {
          originalFunc = func;
          (function(originalFunc, lastFunc) {
            return func = function() {
              originalFunc();
              return setTimeout(lastFunc, 1);
            };
          })(originalFunc, lastFunc);
        }
        lastFunc = func;
        idx++;
      }
      return func();
    }
  };

}).call(this);
